import { ClockDisplay } from '@components/core/ClockDisplay/ClockDisplay'

export const GlobalClock = () => {
  return <ClockDisplay bpm={120} beat={1} onTapClick={() => {}} onResetClick={() => {}} />
}
